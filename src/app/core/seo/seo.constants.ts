/** Production origin — used to build absolute canonical/OG URLs. Must
 * match wherever this app is actually deployed (Cloudflare Pages). */
export const SITE_URL = 'https://tsonova.com';

export const SITE_NAME = 'tSoNova';
